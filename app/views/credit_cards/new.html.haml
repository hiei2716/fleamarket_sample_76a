-# トップページからヘッダーを持ってくる
-# = render 'top/header'

%main.y__l-container.clearfix
  .y__l-container-box
    = render 'mypage/side'
    
    = render 'create'

-# トップページからフッターを持ってくる
-# = render 'top/footer'